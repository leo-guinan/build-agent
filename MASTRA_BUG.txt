The Mastra CLI bundler (npx mastra dev) incorrectly transforms imports from @mastra/libsql to @mastra/core/dist/storage/libsql causing MODULE_NOT_FOUND errors. The bundler should preserve imports from official @mastra scoped packages like @mastra/libsql unchanged, since they are separate npm packages not included in @mastra/core. Fix the import transformation logic to exclude @mastra/* packages from being rewritten.
